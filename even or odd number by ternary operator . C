#include<stdio.h>
#include<conio.h>
int main()
{
	int no = 0;
	
	printf("\n enter a number =  ");
	scanf("%d",&no);
	
	(no == 0) ? printf("\n %d is neutral.",no) : (no % 2 == 0) ? printf("\n %d is even.",no) : printf("\n %d is odd.",no);
	
	printf("\n thanks");
	
	getch();
	return 0;
}